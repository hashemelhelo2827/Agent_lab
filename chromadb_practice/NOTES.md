# TODO: Learn raw chromadb

My LangChain RAG works (third_lab) but LangChain wraps the underlying chromadb.
Learn raw `chromadb` + `sentence-transformers` directly for:
- Custom retrieval logic (filtering, hybrid search)
- Portability (no LangChain dependency)
- Full control over collections, persistence, and embedding models

Resources:
- `chromadb` docs: https://docs.trychroma.com/
- `SentenceTransformer` docs: https://www.sbert.net/
